import type {FastifyInstance} from 'fastify'; import type {AccountPool} from '../../../accounts/pool.js'; import {z} from 'zod'; import {contentToText} from '../../../shared/types.js';
const schema=z.object({model:z.string().optional(),messages:z.array(z.object({role:z.string(),content:z.unknown()})).min(1),stream:z.boolean().optional()});
export function registerChatCompletions(app:FastifyInstance,pool?:AccountPool):void{app.post('/v1/chat/completions',async(req,reply)=>{const body=schema.parse(req.body);if(!pool)return reply.code(503).send({error:{message:'No account pool configured',type:'server_error'}});const prompt=body.messages.map(m=>m.role+': '+contentToText(m.content)).join('\n');if(body.stream){reply.hijack();reply.raw.writeHead(200,{'content-type':'text/event-stream; charset=utf-8','cache-control':'no-cache'});await pool.run(account=>account.client.generate({prompt,model:body.model},event=>{if(event.type==='text')reply.raw.write('data: '+JSON.stringify({id:'chatcmpl-gemini',object:'chat.completion.chunk',choices:[{index:0,delta:{content:event.text??''}}]})+'\n\n');}));reply.raw.write('data: [DONE]\n\n');reply.raw.end();return;}const text=await pool.run(async account=>{let output='';await account.client.generate({prompt,model:body.model},event=>{if(event.type==='text')output+=event.text??''});return output;});return {id:'chatcmpl-gemini',object:'chat.completion',choices:[{index:0,message:{role:'assistant',content:text},finish_reason:'stop'}]};});}
